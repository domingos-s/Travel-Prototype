# Interaction Map

- User clicks `Search` button -> visible primary CTA (no route change in prototype).
- User changes `State` to `Loaded` -> shows destination cards after loading skeleton.
- User changes `State` to `Empty` -> shows no-results message.
- User changes `State` to `Error` -> shows recoverable error banner.
- User taps bottom nav items -> active tab highlight updates.
